# Radar Promo v2.0 - Dependencies
# Diuji dengan Python 3.12+
# Install dengan: python -m pip install -r requirements.txt

# --- GUI Utama ---
PyQt6>=6.6.0
PyQt6-WebEngine>=6.6.0       # Diperlukan untuk tampilan peta (QWebEngineView)
PyQt6-Qt6>=6.6.0             # Qt6 runtime (otomatis terpasang bersama PyQt6)

# --- Visualisasi ---
matplotlib>=3.8.0
Pillow>=10.0.0                # Diperlukan untuk gambar produk & banner
folium>=4.0.0                  # Peta interaktif untuk LokasiPage (QWebEngineView)

# --- Jaringan & Data ---
requests>=2.31.0              # HTTP request ke Google Sheets API & ImgBB
beautifulsoup4>=4.12.0        # Web scraping (dipakai oleh scraper.py)
# Baca kredensial dari file .env lokal
python-dotenv>=1.0.0

# --- Packaging ---
pyinstaller>=6.0.0
